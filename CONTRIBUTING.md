# 贡献指南

欢迎提交 Bug 修复、流程改进、测试和文档优化。

## 开发流程

1. 从 `main` 创建主题分支。
2. 保持 `SKILL.md` 为 Codex 与 Claude Code 共用的唯一流程来源，不复制运行时专用版本。
3. 修改 helper 或安装器时，先补充失败测试，再完成实现。
4. 修改版本或用户可见行为时，同步更新 `VERSION`、`CHANGELOG.md` 和 README/使用指南。
5. 提交前运行：

   ```bash
   bash scripts/check.sh
   ```

## 提交内容

- 说明问题、改动范围和验证结果。
- 不提交密钥、访问令牌、真实请求数据或运行时状态文件。
- 与使用者无关的重构请单独提交，避免掩盖流程语义变更。
