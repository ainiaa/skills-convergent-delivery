# 文字分步交付修复计划

目标：用户要求逐步执行时，始终有独立、可见的文字步骤消息；原生计划面板仅在宿主实际提供工具和回执时作为额外能力。

- T1：将独立的开始/结果消息和文字降级边界写为强制 Skill 契约。
- T2：用行为测试约束触发、步骤边界、失败停止与“文字不等于原生 UI 回执”。
- T3：更新变更日志，运行完整检查。

顺序：T1 → T2 → T3。每项完成后先报告改动和验证，再进入下一项。
