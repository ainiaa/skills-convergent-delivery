interface:
  display_name: "Converge · 单任务闭环交付"
  short_description: "有限收敛一个软件任务，优先委托 PDLC 并以新鲜证据交付"
  default_prompt: "使用 $converge 闭环处理当前单个软件任务：优先复用可用 PDLC，有限修复并用新鲜证据交付。"
policy:
  allow_implicit_invocation: true
